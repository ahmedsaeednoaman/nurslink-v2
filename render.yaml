services:
  - type: web
    name: nurslink
    env: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start
    envVars:
      - key: DATABASE_URL
        value: (اضبط هنا رابط قاعدة البيانات)
